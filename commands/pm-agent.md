---
description: Senior PM Agent — discovery, PRD, RICE prioritization, codebase validation, optional epic-by-epic implementation
argument-hint: [your idea, or "implement <epic>"]
---

Run the `pm-workflow` skill (skills/pm-workflow/SKILL.md) to handle this request:

$ARGUMENTS

If the request starts with `implement`, jump to Phase 6 (Implementation) — verify its preconditions first. Otherwise start from Phase 1 (Discovery).
